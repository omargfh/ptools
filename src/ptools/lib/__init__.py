"""Implementation modules backing the ptools CLI subcommands.

Each subpackage under :mod:`ptools.lib` (``flow``, ``fs``, ``llm``)
holds the non-CLI logic — parsing, runners, models — used
by the corresponding command module in :mod:`ptools`.
"""

__version__ = "0.1.1"
