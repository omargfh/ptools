# Fix scripts/generate_requirements.py drift from full_requirements.txt

**Goal**: Make `scripts/generate_requirements.py`'s output match the committed `full_requirements.txt`, or regenerate `full_requirements.txt` from the current generator.

**In scope**: `scripts/generate_requirements.py`'s binary-requirement rendering and header text; the `tabulate` version floor in whichever of the two is wrong.

**Out of scope**: broader requirements-management redesign; changing how optional-dependency announcement works (`ptools.utils.require`).

**Description**: `full_requirements.txt` states it is "generated by scripts/generate_requirements.py" and must not be hand-edited, but running the script now (verified 2026-07-14) produces a diff against the committed file:
- Header punctuation differs ("- generated by" / "NOT referenced from pyproject.toml -" vs no dashes in the live output).
- The live output repeats `darwin` as a fake OS-binary requirement four times; the committed file has none.
- The live output pins `tabulate>=0.10.0` (matching `pyproject.toml`); the committed file has `tabulate>=0.8.0`.

Until this is fixed, the file can't actually be regenerated as its own header instructs.

**Acceptance criteria**:
- `python scripts/generate_requirements.py` output matches the committed `full_requirements.txt` byte-for-byte (or `full_requirements.txt` is regenerated to match and committed alongside the fix).
- No duplicate/placeholder OS binary entries (e.g. repeated `darwin`) appear in generator output.

**Depends on**: none

**Notes**: exact diff captured during scaffolding on 2026-07-14; re-run `diff <(python scripts/generate_requirements.py) full_requirements.txt` to reproduce.

**Status**: proposed — not approved
